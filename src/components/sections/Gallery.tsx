// Phase 3 实现：Masonry 瀑布流，悬停放大 / 视差滚动
const Gallery = () => {
  return (
    <section
      id="gallery"
      className="flex min-h-screen flex-col items-center justify-center bg-surface"
    >
      <h2 className="text-3xl font-semibold text-text">Gallery</h2>
      <p className="mt-3 text-text-muted">个人照片墙 — Phase 3 实现</p>
    </section>
  );
};

export default Gallery;
