// Phase 3 实现：卡片式布局，调用 GitHub API 拉取仓库数据
const Projects = () => {
  return (
    <section
      id="projects"
      className="flex min-h-screen flex-col items-center justify-center bg-surface"
    >
      <h2 className="text-3xl font-semibold text-text">Projects</h2>
      <p className="mt-3 text-text-muted">项目展示 — Phase 3 实现</p>
    </section>
  )
}

export default Projects
